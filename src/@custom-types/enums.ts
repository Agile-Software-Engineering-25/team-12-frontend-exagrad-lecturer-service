export enum UserType {
  LECTURER = 'LECTURER',
  STUDENT = 'STUDENT',
  ADMIN = 'ADMIN',
}

export enum ExamType {
  EXAM = 'EXAM',
  PRESENTATION = 'PRESENTATION',
  ORAL = 'ORAL',
  PROJECT = 'PROJECT',
  OTHERS = 'OTHERS',
}

export enum ExamGradingState {
  GRADED = 'graded',
  UNGRADED = 'ungraded',
  PARTIALLY = 'partial',
}
